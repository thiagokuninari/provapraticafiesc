
spring:
  jpa:
    database: ORACLE
    show-sql: false
    hibernate:
      ddl-auto: none

  datasource:
    #TODO por enquanto vai rodar apontando pro banco de homologação, até o prod ficar pronto
    url: jdbc:oracle:thin:@//192.168.1.7:1521/hmlg
    username: AUTENTICACAO
    password: py12nrk7
    driverClassName: oracle.jdbc.OracleDriver
    maximumPoolSize: 2 #TODO quando entrar o banco de prod, setar 30

  rabbitmq:
    host: 170.10.10.60
    port: 5672
    username: xbrain
    password: xbrain

app-config:
  url: https://autenticacao.conexaoclarobrasil.com.br
  url-estatico-upload: ${app-config.url-estatico}/producao/autenticacao
  upload-dir: ${app-config.estatico-dir}/producao/autenticacao

  email:
    enviar: true
    emails:

  services:
    call:
      url: http://172.10.10.78:8094
    parceiros-online:
      url: http://172.10.10.76:8099

logging:
  level:
      org.springframework: 'OFF'
      org.hibernate: 'OFF'