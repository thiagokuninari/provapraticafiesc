

app-config:
  url: https://autenticacao.conexaoclarobrasil.com.br
  url-estatico-upload: ${app-config.url-estatico}/producao/autenticacao
  upload-dir: ${app-config.estatico-dir}/producao/autenticacao

  email:
    enviar: true
    emails:

  services:
    call:
      url: https://call.conexaoclarobrasil.com.br

spring:
  resources:
    chain:
      cache: false
  jpa:
    database: ORACLE
    show-sql: false
    hibernate:
      ddl-auto: none

  datasource:
    url: jdbc:oracle:thin:@//192.168.1.205:1521/xprod12
    username: AUTENTICACAO
    password: 4ut3nt1c4c40
    driverClassName: oracle.jdbc.OracleDriver
    maximumPoolSize: 2

  rabbitmq:
      host: 192.168.1.240
      port: 5671
      username: xbrain
      password: xbrain